<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ChronosFlow</title>
    <link rel="icon"
        href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>⏳</text></svg>">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght=300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link rel="stylesheet" href="/css/homePageStyles.css">

    <style>
        /* ==========================================================================
       📱 1. LOGIC CO GIÃN SIDEBAR & HIỆU ỨNG TỊNH TIẾN (TRANSITION)
       ========================================================================== */
        .sidebar {
            transition: width 0.3s cubic-bezier(0.4, 0, 0.2, 1), padding 0.3s ease !important;
            overflow-x: hidden !important;
        }

        .main-content {
            transition: padding-left 0.3s cubic-bezier(0.4, 0, 0.2, 1), margin-left 0.3s cubic-bezier(0.4, 0, 0.2, 1) !important;
        }

        /* Khi Sidebar bị co lại (Collapsed) */
        .sidebar.collapsed {
            width: 75px !important;
            padding-left: 8px !important;
            padding-right: 8px !important;
        }

        .sidebar.collapsed~.main-content,
        body:has(.sidebar.collapsed) .main-content {
            margin-left: 75px !important;
            padding-left: 20px !important;
        }

        .sidebar.collapsed .menu-text,
        .sidebar.collapsed .brand-logo img,
        .sidebar.collapsed .btn-logout span>span {
            display: none !important;
        }

        .sidebar.collapsed .brand-logo::before {
            content: "⏳";
            font-size: 24px;
            display: block;
        }

        .sidebar.collapsed .sidebar-header-area {
            flex-direction: column-reverse !important;
            gap: 12px !important;
        }

        .sidebar.collapsed .nav-menu .nav-item a {
            display: flex !important;
            justify-content: center !important;
            align-items: center !important;
            width: 50px !important;
            height: 45px !important;
            padding: 0 !important;
            border-radius: 8px !important;
        }

        .sidebar.collapsed .btn-logout {
            justify-content: center !important;
            display: flex;
        }


        /* ==========================================================================
       🚨 2. HỆ THỐNG BIẾN MÀU CENTRAL CENTRAL CONTROL UNIT
       ========================================================================== */

        /* CHẾ ĐỘ SÁNG MẶC ĐỊNH (LIGHT MODE) */
        :root,
        body {
            --bg-main: #f8fafc;
            /* Nền chính xám nhạt nhẹ nhàng */
            --bg-card: #ffffff;
            /* Nền các khối card trắng tinh */
            --text-main: #1e293b;
            /* Chữ chính màu xanh đen đậm */
            --text-muted: #64748b;
            /* Chữ phụ màu xám */
            --bg-input: #ffffff;
            /* Nền các ô input, select */
            --border-color: #e2e8f0;
            /* Viền kẻ nhạt */
        }

        /* CHẾ ĐỘ TỐI KHI KÍCH HOẠT (DARK MODE) */
        body.dark-mode-active {
            --bg-main: #0f172a;
            /* Nền tối sâu thẳm (slate-900) */
            --bg-card: #1e293b;
            /* Nền các khối card tối vừa (slate-800) */
            --text-main: #f8fafc;
            /* Chữ trắng sáng dễ đọc */
            --text-muted: #94a3b8;
            /* Chữ phụ xám sáng */
            --bg-input: #334155;
            /* Nền input tối (slate-700) */
            --border-color: #334155;
            /* Viền kẻ tối */
        }


        /* ==========================================================================
   🚨 HỆ THỐNG BIẾN MÀU CENTRAL DARK/LIGHT MODE ĐỒNG BỘ TOÀN DIỆN CHRONOSFLOW
   ========================================================================== */

        /* 1. CHẾ ĐỘ SÁNG MẶC ĐỊNH (LIGHT MODE) */
        :root {
            --bg-main: #f8fafc;
            --bg-card: #ffffff;
            --text-main: #1e293b;
            --text-muted: #475569;
            --bg-input: #ffffff;
            --border-color: #e2e8f0;
        }

        /* 2. CHẾ ĐỘ TỐI KHI KÍCH HOẠT (DARK MODE) */
        body.dark-mode-active {
            --bg-main: #0f172a;
            --bg-card: #1e293b;
            --text-main: #f8fafc;
            --text-muted: #cbd5e1;
            --bg-input: #334155;
            --border-color: #334155;
        }

        /* ==========================================================================
   🧱 ÉP TOÀN BỘ CẤU TRÚC CÁC TRANG CON ĂN THEO BIẾN MÀU HỆ THỐNG
   ========================================================================== */

        body {
            background-color: var(--bg-main) !important;
            color: var(--text-main) !important;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* Ép vùng chứa nội dung của TẤT CẢ các phân hệ Tab con */
        .main-content,
        .tab-content,
        .dashboard-wrapper,
        .task-management-wrapper,
        .calendar-dashboard-layout {
            background-color: var(--bg-main) !important;
            color: var(--text-main) !important;
        }

        /* 🚨 ĐÈ MÀU KHỐI CARD CHO CHẾ ĐỘ DARK MODE (SỬA LỖI TRẮNG NỀN) */
        body.dark-mode-active div[style*="background: #fff"],
        body.dark-mode-active div[style*="background: white"],
        body.dark-mode-active div[style*="background-color: #fff"],
        body.dark-mode-active div[style*="background-color: white"],
        body.dark-mode-active .stat-card,
        body.dark-mode-active .chart-block-left,
        body.dark-mode-active .chart-block-center,
        body.dark-mode-active .chart-block-right,
        body.dark-mode-active .settings-wrapper,
        body.dark-mode-active .task-form-card,
        body.dark-mode-active .category-filters,
        body.dark-mode-active .calendar-main-board,
        body.dark-mode-active .kanban-column,
        body.dark-mode-active #schedule-form-card,
        body.dark-mode-active .calendar-sidebar>div {
            background-color: var(--bg-card) !important;
            background: var(--bg-card) !important;
            color: var(--text-main) !important;
            border-color: var(--border-color) !important;
        }

        /* 📅 1. Tab Lịch trình (Schedules - Xử lý triệt để FullCalendar lồng nhau) */
        body.dark-mode-active .fc,
        body.dark-mode-active .fc-view-harness,
        body.dark-mode-active .fc-view-harness *,
        body.dark-mode-active .fc-scrollgrid,
        body.dark-mode-active .fc-scroller,
        body.dark-mode-active .fc-daygrid-day-frame,
        body.dark-mode-active .fc-daygrid-day-top,
        body.dark-mode-active .fc-daygrid-day,
        body.dark-mode-active td.fc-daygrid-day {
            background-color: var(--bg-card) !important;
            background: var(--bg-card) !important;
            border-color: var(--border-color) !important;
        }

        body.dark-mode-active .fc-theme-standard td,
        body.dark-mode-active .fc-theme-standard th {
            border: 1px solid var(--border-color) !important;
        }

        body.dark-mode-active .fc-day-other {
            background-color: var(--bg-main) !important;
            opacity: 0.5;
        }

        /* 📚 2. Tab Học tập (Learning Hub - Sửa tận gốc ô môn học sinh động từ JS) */
        body.dark-mode-active #subjects-list-group div,
        body.dark-mode-active #subjects-list-group div * {
            background-color: var(--bg-card) !important;
            background: var(--bg-card) !important;
            color: var(--text-main) !important;
            border-color: var(--border-color) !important;
        }

        /* 📁 3. Tab Công việc (Tasks) */
        body.dark-mode-active select#proj-priority,
        body.dark-mode-active select#task-priority,
        body.dark-mode-active select#sched-category,
        body.dark-mode-active select#sched-type {
            background-color: var(--bg-input) !important;
            color: var(--text-main) !important;
        }

        body.dark-mode-active span[style*="background: #fff"],
        body.dark-mode-active span[style*="background: white"] {
            background-color: var(--bg-input) !important;
            color: var(--text-main) !important;
            border: 1px solid var(--border-color) !important;
        }

        /* 💰 4. Tab Tài chính (Finance Flow) */
        body.dark-mode-active thead[style*="background: #f1f5f9"],
        body.dark-mode-active thead[style*="background: #f1f5f9"] tr,
        body.dark-mode-active thead[style*="background: #f1f5f9"] th {
            background-color: var(--bg-main) !important;
        }

        /* ==========================================================================
   🖋️ 5. ÉP CHỮ VÀ INPUT ĐỔI MÀU (ĐÃ SỬA LỖI TÁCH BIỆT BỘ CHỌN)
   ========================================================================== */

        /* Ép chữ ở chế độ Dark Mode */
        body.dark-mode-active h1,
        body.dark-mode-active h2,
        body.dark-mode-active h3,
        body.dark-mode-active p,
        body.dark-mode-active span,
        body.dark-mode-active label,
        body.dark-mode-active strong,
        body.dark-mode-active th,
        body.dark-mode-active td,
        body.dark-mode-active i,
        body.dark-mode-active .fc-toolbar-title,
        body.dark-mode-active .fc-daygrid-day-number,
        body.dark-mode-active .fc-col-header-cell-cushion {
            color: var(--text-main) !important;
        }

        /* Ép chữ mờ ở chế độ Dark Mode */
        body.dark-mode-active .text-muted,
        body.dark-mode-active p[style*="color: #6c757d"],
        body.dark-mode-active span[style*="color: #64748b"] {
            color: var(--text-muted) !important;
        }

        /* Nhuộm tối toàn bộ các thẻ Input, Textarea, Select */
        body.dark-mode-active input,
        body.dark-mode-active select,
        body.dark-mode-active textarea {
            background-color: var(--bg-input) !important;
            color: var(--text-main) !important;
            border: 1px solid var(--border-color) !important;
        }
    </style>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>

<body>

    <aside class="sidebar" id="main-sidebar">
        <div>
            <div style="display: flex; flex-direction: column; align-items: center; justify-content: space-between; margin-bottom: 20px;"
                class="sidebar-header-area">
                <div class="brand-logo" style="margin: 0;">
                    <img src="/images/Dark-Logo.png" alt="ChronosFlow Logo">
                </div>
            </div>
            <button class="nav-item" onclick="toggleSidebar()"
                style="background: transparent; border: none; color: #94a3b8; cursor: pointer; font-size: 18px; padding: 5px; display: flex; align-items: center;">
                <a href="#"><i class="fa-solid fa-bars" id="sidebar-toggle-icon"></i><span
                        class="menu-text">Menu</span></a>
            </button>
            <ul class="nav-menu">
                <li class="nav-item active" data-tab="dashboard"><a href="#"><i class="fa-solid fa-house"></i><span
                            class="menu-text">Dashboard</span></a></li>
                <li class="nav-item" data-tab="schedules"><a href="#"><i class="fa-solid fa-calendar"></i><span
                            class="menu-text">Schedules</span></a></li>
                <li class="nav-item" data-tab="tasks"><a href="#"><i class="fa-solid fa-list-check"></i><span
                            class="menu-text">Tasks</span></a></li>
                <li class="nav-item" data-tab="learning"><a href="#"><i class="fa-solid fa-book"></i><span
                            class="menu-text">Learning</span></a></li>
                <li class="nav-item" data-tab="finance"><a href="#"><i class="fa-solid fa-money-bill-wave"></i><span
                            class="menu-text">Finance</span></a></li>
                <li class="nav-item" data-tab="settings"><a href="#"><i class="fa-solid fa-gear"></i><span
                            class="menu-text">Settings</span></a></li>
            </ul>
        </div>
        <a href="/login" class="btn-logout">
            <span><i class="fa-solid fa-arrow-right-from-bracket"></i><span>Sign Out</span></span>
        </a>
    </aside>

    <main class="main-content">
        <div id="dashboard" class="tab-content active">
            <%- include('partials/dashboardSection') %>
        </div>
        <div id="schedules" class="tab-content"><%- include('partials/scheduleSection') %></div>
        <div id="tasks" class="tab-content"><%- include('partials/tasksSection') %></div>
        <div id="learning" class="tab-content"><%- include('partials/learningSection') %></div>
        <div id="finance" class="tab-content"><%- include('partials/financeSection') %></div>
        <div id="settings" class="tab-content"><%- include('partials/settingSection') %></div>
    </main>

    <script>
        // 🔄 1. BIẾN TOÀN CỤC KHỞI TẠO ĐẦU TIÊN
        let lineChartInstance = null;
        let donutChartInstance = null;

        // 📊 2. ĐỊNH NGHĨA HÀM ENGINE TRƯỚC ĐỂ TRÌNH DUYỆT KHÔNG BÁO "NOT DEFINED"
        function fetchDashboardStats() {
            fetch('/api/dashboard-stats')
                .then(response => {
                    if (!response.ok) throw new Error("API Network Response Error");
                    return response.json();
                })
                .then(data => {
                    // Đổ số liệu 4 Mini Cards đỉnh
                    if (document.getElementById('stat-finance-balance')) document.getElementById('stat-finance-balance').innerText = (data.financeBalance || 0).toLocaleString('vi-VN') + " VND";
                    if (document.getElementById('stat-active-tasks')) document.getElementById('stat-active-tasks').innerText = (data.activeTasks || 0) + " Tasks";
                    if (document.getElementById('stat-total-projects')) document.getElementById('stat-total-projects').innerText = (data.totalProjects || 0) + " Projects";
                    if (document.getElementById('stat-learning-cards')) document.getElementById('stat-learning-cards').innerText = (data.learningCoursesCount || 0) + " Courses";

                    // Khởi tạo đồ thị sóng (Line Chart)
                    const ctxLine = document.getElementById('dashboardLineChart');
                    if (ctxLine) {
                        if (lineChartInstance) lineChartInstance.destroy();
                        lineChartInstance = new Chart(ctxLine, {
                            type: 'line',
                            data: {
                                labels: ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'],
                                datasets: [{
                                    label: 'Accumulated Study Hours',
                                    data: data.learningHoursDataset || [0, 0, 0, 0, 0, 0, 0],
                                    borderColor: '#2563eb',
                                    backgroundColor: 'rgba(37, 99, 235, 0.1)',
                                    tension: 0.4,
                                    fill: true
                                }]
                            },
                            options: { responsive: true, maintainAspectRatio: false }
                        });
                    }

                    // Khởi tạo đồ thị tròn (Donut Chart)
                    const ctxDonut = document.getElementById('dashboardDonutChart');
                    if (ctxDonut) {
                        if (donutChartInstance) donutChartInstance.destroy();
                        donutChartInstance = new Chart(ctxDonut, {
                            type: 'doughnut',
                            data: {
                                labels: ['High', 'Medium', 'Low'],
                                datasets: [{
                                    data: data.taskPriorityDataset || [0, 0, 0],
                                    backgroundColor: ['#ef4444', '#f59e0b', '#3b82f6'],
                                    borderWidth: 2
                                }]
                            },
                            options: { responsive: true, maintainAspectRatio: false }
                        });
                    }

                    // Bơm dữ liệu lịch trình thật vào hộp Agenda
                    const container = document.getElementById('dashboard-today-schedule-list');
                    if (container) {
                        container.innerHTML = '';
                        const todaySchedules = data.todaySchedules || [];

                        if (todaySchedules.length === 0) {
                            container.innerHTML = `<p style="font-size: 12px; color: #94a3b8; text-align: center; margin: auto 0; padding: 20px 0;">🎯 Today is free! No scheduled events.</p>`;
                        } else {
                            todaySchedules.forEach(item => {
                                container.insertAdjacentHTML('beforeend', `
                                    <div style="background: #f8fafc; padding: 10px; border-radius: 8px; border-left: 4px solid #2563eb; display: flex; align-items: center; gap: 10px; font-size: 12px; margin-bottom: 8px;">
                                        <strong style="color: #475569; min-width: 45px;">⏱ ${item.start_time || '00:00'}</strong>
                                        <span style="color: #1e293b; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${item.title}</span>
                                    </div>
                                `);
                            });
                        }
                    }
                })
                .catch(err => console.error('Lỗi API Core Render:', err));
            // Thêm đoạn code này vào cuối sự kiện DOMContentLoaded (Ngay dưới dòng loadUserSettings();)
            document.addEventListener("DOMContentLoaded", () => {
                startLiveClock();
                if (typeof fetchDashboardStats === 'function') fetchDashboardStats();
                if (typeof loadUserSettings === 'function') loadUserSettings();

                // 🛡️ BỘ ĐIỀU KHIỂN ÉP MÀU SCHEDULES TRỰC TIẾP BẰNG JAVASCRIPT
                const syncScheduleDarkMode = () => {
                    const isDarkMode = document.body.classList.contains('dark-mode-active');

                    // 1. Tìm tất cả các ô lưới lịch và khung bọc FullCalendar
                    const calendarElements = document.querySelectorAll('.fc, .fc-view-harness, .fc-scrollgrid, .fc-scroller, .fc-daygrid-day-frame, .fc-daygrid-day-top, .fc-daygrid-day, td.fc-daygrid-day');
                    calendarElements.forEach(el => {
                        el.style.setProperty('background-color', isDarkMode ? '#1e293b' : '#ffffff', 'important');
                        el.style.setProperty('background', isDarkMode ? '#1e293b' : '#ffffff', 'important');
                        el.style.setProperty('border-color', isDarkMode ? '#334155' : '#e2e8f0', 'important');
                    });

                    // 2. Sửa lỗi mất màu chữ Thời gian/Ngày tháng (Chữ số 1, 2, 3 và chữ Thứ)
                    const calendarTexts = document.querySelectorAll('.fc-daygrid-day-number, .fc-col-header-cell-cushion, .fc-toolbar-title, #calendar-view-title');
                    calendarTexts.forEach(el => {
                        el.style.setProperty('color', isDarkMode ? '#f8fafc' : '#1e293b', 'important');
                    });

                    // 3. Sửa lỗi mất màu chữ Đồng hồ bên Schedules (Ép chữ màu trắng sáng ở darkmode)
                    const clockWidget = document.querySelector('.calendar-sidebar div[style*="background: #fff"] h2, #live-time');
                    const clockLabels = document.querySelectorAll('.calendar-sidebar div[style*="background: #fff"] div, #live-date');

                    if (clockWidget) clockWidget.style.setProperty('color', isDarkMode ? '#f8fafc' : '#1e293b', 'important');
                    clockLabels.forEach(el => el.style.setProperty('color', isDarkMode ? '#cbd5e1' : '#64748b', 'important'));
                };

                // Lắng nghe nút gạt Dark Mode để ép màu lịch chạy real-time
                const darkModeCheck = document.getElementById('settings-darkmode');
                if (darkModeCheck) {
                    darkModeCheck.addEventListener('change', () => {
                        // Đợi 50ms để class body được cập nhật xong rồi ép màu
                        setTimeout(syncScheduleDarkMode, 50);
                    });
                }

                // Mỗi khi người dùng bấm chuyển sang tab Schedules, chạy hàm ép màu để phòng hờ lịch load chậm
                document.querySelectorAll('.nav-menu .nav-item').forEach(item => {
                    item.addEventListener('click', () => {
                        setTimeout(syncScheduleDarkMode, 150);
                    });
                });

                // Chạy kích hoạt lần đầu tiên sau khi trang nạp xong
                setTimeout(syncScheduleDarkMode, 500);
            });
        }

        // ⚙️ 3. CÁC HÀM TIỆN ÍCH SIDEBAR VÀ ĐỒNG HỒ
        function toggleSidebar() {
            const sidebar = document.getElementById('main-sidebar');
            const toggleIcon = document.getElementById('sidebar-toggle-icon');
            if (!sidebar || !toggleIcon) return;

            // Bật/Tắt class collapsed
            sidebar.classList.toggle('collapsed');

            // 🚨 VÁ LỖI CỐT LÕI: Thêm đầy đủ class 'fa-solid fa-bars' để icon không bị biến mất
            toggleIcon.className = sidebar.classList.contains('collapsed')
                ? 'fa-solid fa-angles-right'
                : 'fa-solid fa-bars';
        }
        function startLiveClock() {
            const timeEl = document.getElementById('live-time');
            const dateEl = document.getElementById('live-date');
            if (!timeEl || !dateEl) return;
            setInterval(() => {
                const now = new Date();
                timeEl.innerText = now.toLocaleTimeString('en-US', { hour12: false });
                dateEl.innerText = `⏳ ${now.toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'short', day: '2-digit' })}`;
            }, 1000);
        }

        // 🔔 4. LẮNG NGHE SỰ KIỆN KHỞI CHẠY (BỌC THÉP TRÁNH CRASH)
        document.addEventListener("DOMContentLoaded", () => {
            startLiveClock();
            // Đảm bảo hàm đã tồn tại trong bộ nhớ rồi mới kích hoạt gọi
            if (typeof fetchDashboardStats === 'function') {
                fetchDashboardStats();
            }
        });

        // 💡 5. LOGIC CHUYỂN TÁP ĐIỀU HƯỚNG SIDEBAR
        document.querySelectorAll('.nav-menu .nav-item').forEach(item => {
            item.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector('.nav-menu .nav-item.active')?.classList.remove('active');
                item.classList.add('active');

                const targetTabId = item.getAttribute('data-tab');
                document.querySelector('.tab-content.active')?.classList.remove('active');
                document.getElementById(targetTabId)?.classList.add('active');

                if (targetTabId === 'dashboard' && typeof fetchDashboardStats === 'function') {
                    fetchDashboardStats();
                }
            });
        });
        // Thêm vào sự kiện DOMContentLoaded để tự động tải cấu hình cũ lên
        document.addEventListener("DOMContentLoaded", () => {
            startLiveClock();
            if (typeof fetchDashboardStats === 'function') fetchDashboardStats();
            if (typeof loadUserSettings === 'function') loadUserSettings();

            // 🛡️ BỘ ĐIỀU KHIỂN CSS INJECTION ĐẬP TAN VẾT TRẮNG FULLCALENDAR VÀ LEARNING HUB
            // 🛡️ CẬP NHẬT BỘ ĐIỀU KHIỂN CSS INJECTION DỌN SẠCH VẾT TRẮNG CUỐI CÙNG
            // 🛡️ BỘ ĐIỀU KHIỂN CSS INJECTION NÂNG CẤP BỌC THÉP TOÀN DIỆN TẬN GỐC
            // 🛡️ BỘ ĐIỀU KHIỂN CSS INJECTION - BẢN THẮNG LỢI HOÀN TOÀN
            const injectDynamicDarkModeCSS = () => {
                let dynamicStyle = document.getElementById('chronosflow-dynamic-dark-css');
                if (dynamicStyle) dynamicStyle.remove();

                dynamicStyle = document.createElement('style');
                dynamicStyle.id = 'chronosflow-dynamic-dark-css';

                dynamicStyle.innerHTML = `
            /* 📅 1. PHÂN HỆ LỊCH TRÌNH (Schedules - Định danh chính xác class cấu trúc) */
            body.dark-mode-active .calendar-top-bar,
            body.dark-mode-active .fc,
            body.dark-mode-active .fc-header-toolbar,
            body.dark-mode-active .fc-toolbar-chunk,
            body.dark-mode-active .view-switcher,
            body.dark-mode-active .fc-view-harness,
            body.dark-mode-active .fc-scrollgrid,
            body.dark-mode-active .fc-scroller,
            body.dark-mode-active .fc-daygrid-day,
            body.dark-mode-active .fc-daygrid-day-frame,
            body.dark-mode-active .fc-theme-standard td,
            body.dark-mode-active .fc-theme-standard th,
            body.dark-mode-active .calendar-sidebar > div,
            body.dark-mode-active .category-filters {
                background-color: #1e293b !important;
                background: #1e293b !important;
                border-color: #334155 !important;
            }

            /* TRẢ LẠI NỀN TRONG SUỐT CHO CÁC THẺ CON TRONG LƯỚI ĐỂ TRÁNH LỖI THANH ĐÈ SỐ NGÀY */
            body.dark-mode-active .fc-daygrid-day-top,
            body.dark-mode-active .fc-daygrid-day-number,
            body.dark-mode-active .fc-daygrid-day-events,
            body.dark-mode-active .fc-daygrid-day-bg,
            body.dark-mode-active .fc-col-header-cell,
            body.dark-mode-active .fc-col-header-cell-cushion {
                background: transparent !important;
                background-color: transparent !important;
            }

            /* 🚨 ÉP MÀU NỀN TỐI VÀ CHỮ TRẮNG CHO NÚT BẤM ĐIỀU HƯỚNG (<, >, Today, Month...) */
            body.dark-mode-active .fc-button,
            body.dark-mode-active .fc-button-primary,
            body.dark-mode-active .view-switcher button:not(.active) {
                background-color: #334155 !important;
                background: #334155 !important;
                color: #f8fafc !important;
                border: 1px solid #475569 !important;
                opacity: 1 !important;
            }

            /* 🚨 ÉP ICON MŨI TÊN BÊN TRONG NÚT BẤM HIỆN RÕ MÀU TRẮNG */
            body.dark-mode-active .fc-button .fc-icon {
                color: #f8fafc !important;
                background: transparent !important;
                background-color: transparent !important;
            }

            /* Hiệu ứng Hover mượt mà khi di chuột vào nút trên lịch */
            body.dark-mode-active .fc-button:hover,
            body.dark-mode-active .fc-button:active {
                background-color: #2563eb !important;
                background: #2563eb !important;
                border-color: #2563eb !important;
                color: #ffffff !important;
            }

            /* Giữ màu chữ hiển thị chuẩn chỉ */
            body.dark-mode-active .fc-daygrid-day-number,
            body.dark-mode-active .fc-col-header-cell-cushion,
            body.dark-mode-active .fc-toolbar-title,
            body.dark-mode-active #calendar-view-title {
                color: #f8fafc !important;
            }

            /* Xử lý các ngày của tháng trước/sau (ngày mờ) */
            body.dark-mode-active .fc-day-other {
                background-color: #0f172a !important;
                background: #0f172a !important;
                opacity: 0.4;
            }

            /* 💰 2. PHÂN HỆ TÀI CHÍNH (Finance Flow) */
            body.dark-mode-active table thead,
            body.dark-mode-active table thead tr,
            body.dark-mode-active table thead th,
            body.dark-mode-active tr[style*="background: #f8fafc"] {
                background-color: #0f172a !important;
                background: #0f172a !important;
                color: #f8fafc !important;
            }

            /* 📚 3. PHÂN HỆ HỌC TẬP (Learning Hub) */
            body.dark-mode-active #subjects-list-group div,
            body.dark-mode-active #subjects-list-group div *,
            body.dark-mode-active .learning-panel div[style*="background: #fff"],
            body.dark-mode-active .learning-panel div[style*="background-color: white"] {
                background-color: #1e293b !important;
                background: #1e293b !important;
                color: #f8fafc !important;
                border-color: #334155 !important;
            }

            /* 📁 4. PHÂN HỆ CÔNG VIỆC (Tasks) */
            body.dark-mode-active .task-form-card,
            body.dark-mode-active .kanban-column,
            body.dark-mode-active .task-list div,                
            body.dark-mode-active .task-item,                    
            body.dark-mode-active button[onclick*="backToProjectHub"],
            body.dark-mode-active .task-list span,               
            body.dark-mode-active .task-list i,                  
            body.dark-mode-active span[style*="background: #fff"],
            body.dark-mode-active span[style*="background: white"],
            body.dark-mode-active div[style*="background: #fff"] span,
            body.dark-mode-active div[style*="background: white"] span {
                background-color: #334155 !important;            
                background: #334155 !important;
                color: #f8fafc !important;                       
                border-color: #475569 !important;
            }

            body.dark-mode-active .task-form-card form,
            body.dark-mode-active section[style*="background: #f8fafc"] {
                background-color: #0f172a !important;
                background: #0f172a !important;
            }

            /* 🖋========= 5. CHỮ VÀ INPUT ĐỔI MÀU TỔNG THỂ ========= */
            body.dark-mode-active h1, body.dark-mode-active h2, body.dark-mode-active h3, 
            body.dark-mode-active h4, body.dark-mode-active h5, body.dark-mode-active p, 
            body.dark-mode-active span, body.dark-mode-active label, body.dark-mode-active strong, 
            body.dark-mode-active th, body.dark-mode-active td, body.dark-mode-active i, 
            body.dark-mode-active #live-time {
                color: #f8fafc !important;
            }

            body.dark-mode-active .text-muted, 
            body.dark-mode-active p[style*="color: #6c757d"],
            body.dark-mode-active span[style*="color: #64748b"],
            body.dark-mode-active .calendar-sidebar div,
            body.dark-mode-active #live-date {
                color: #94a3b8 !important;
            }

            body.dark-mode-active input, body.dark-mode-active select, body.dark-mode-active textarea {
                background-color: #334155 !important;
                color: #f8fafc !important;
                border: 1px solid #334155 !important;
            }
        `;

                document.head.appendChild(dynamicStyle);
            };


            // Lắng nghe sự kiện gạt nút Dark Mode để kích hoạt nạp thẻ style
            const darkModeCheck = document.getElementById('settings-darkmode');
            if (darkModeCheck) {
                darkModeCheck.addEventListener('change', () => {
                    injectDynamicDarkModeCSS();
                });
            }

            // Chạy kích hoạt nạp luật CSS lần đầu tiên ngay khi vào trang
            injectDynamicDarkModeCSS();
        });

        // Hàm lấy dữ liệu cài đặt từ API đổ vào Form
        function loadUserSettings() {
            fetch('/api/user-settings')
                .then(res => res.json())
                .then(config => {
                    if (config.username) document.getElementById('settings-username').value = config.username;

                    // Cập nhật Dark Mode
                    const darkModeCheck = document.getElementById('settings-darkmode');
                    if (darkModeCheck) {
                        darkModeCheck.checked = config.darkMode || false;
                        if (config.darkMode) document.body.classList.add('dark-mode-active');
                    }

                    // Cập nhật Trạng thái Email
                    const emailToggle = document.getElementById('settings-email-toggle');
                    if (emailToggle) {
                        emailToggle.checked = config.allowEmail || false;
                        const freqGroup = document.getElementById('email-frequency-group');
                        if (freqGroup) {
                            freqGroup.style.opacity = config.allowEmail ? "1" : "0.5";
                            freqGroup.style.pointerEvents = config.allowEmail ? "auto" : "none";
                        }
                    }

                    if (config.emailFrequency) document.getElementById('settings-email-frequency').value = config.emailFrequency;
                })
                .catch(err => console.error("Không thể tải cài đặt:", err));
        }

        // Hàm gửi dữ liệu cài đặt mới lên API Backend để lưu
        function saveUserSettings() {
            const payload = {
                username: document.getElementById('settings-username').value,
                darkMode: document.getElementById('settings-darkmode').checked,
                allowEmail: document.getElementById('settings-email-toggle').checked,
                emailFrequency: document.getElementById('settings-email-frequency').value
            };

            fetch('/api/user-settings', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
            })
                .then(res => res.json())
                .then(result => {
                    if (result.success) {
                        alert("🎉 Settings saved successfully!");
                        // Đổi giao diện Dark Mode lập tức mà không cần F5 trang
                        if (payload.darkMode) {
                            document.body.classList.add('dark-mode-active');
                        } else {
                            document.body.classList.remove('dark-mode-active');
                        }
                    }
                })
                .catch(err => alert("🚨 Failed to save settings."));
        }
        // Thay thế đoạn lắng nghe sự kiện Settings cũ trong DomContentLoaded thành:
        document.addEventListener("DOMContentLoaded", () => {
            startLiveClock();
            if (typeof fetchDashboardStats === 'function') fetchDashboardStats();

            // Tải cấu hình cài đặt từ API
            loadUserSettings();

            // 🟢 BỌC AN TOÀN 1: Logic gạt nút đổi màu Dark Mode lập tức real-time
            const darkModeCheck = document.getElementById('settings-darkmode');
            if (darkModeCheck) {
                darkModeCheck.addEventListener('change', () => {
                    if (darkModeCheck.checked) {
                        document.body.classList.add('dark-mode-active');
                    } else {
                        document.body.classList.remove('dark-mode-active');
                    }
                });
            }

            // 🟢 BỌC AN TOÀN 2: Logic ẩn/hiện độ mờ tần suất gửi email
            const emailToggle = document.getElementById('settings-email-toggle');
            const freqGroup = document.getElementById('email-frequency-group');
            if (emailToggle && freqGroup) {
                emailToggle.addEventListener('change', () => {
                    freqGroup.style.opacity = emailToggle.checked ? "1" : "0.5";
                    freqGroup.style.pointerEvents = emailToggle.checked ? "auto" : "none";
                });
            }

            // 🟢 BỌC AN TOÀN 3: Lắng nghe sự kiện submit form để lưu lại
            const settingsForm = document.getElementById('settings-form');
            if (settingsForm) {
                settingsForm.addEventListener('submit', (e) => {
                    e.preventDefault();
                    saveUserSettings();
                });
            }
        });
        function startLiveClock() {
            // Thay vì getElementById (chỉ lấy 1 cái), ta quét tất cả các phần tử có ID này trên toàn trang
            setInterval(() => {
                const now = new Date();
                const timeString = now.toLocaleTimeString('en-US', { hour12: false });
                const dateString = `⏳ ${now.toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'short', day: '2-digit' })}`;

                // Bơm thời gian vào tất cả các thẻ có id là live-time và live-date xuất hiện trên UI
                document.querySelectorAll('#live-time').forEach(el => el.innerText = timeString);
                document.querySelectorAll('#live-date').forEach(el => el.innerText = dateString);
            }, 1000);
        }

    </script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <script>
        document.addEventListener("DOMContentLoaded", function () {
            // 1. Gọi API bốc dữ liệu tổng hợp từ Server của Tuấn
            fetch('/api/dashboard/stats')
                .then(res => {
                    if (!res.ok) throw new Error("Chưa đăng nhập hoặc phiên làm việc hết hạn");
                    return res.json();
                })
                .then(data => {
                    // Cập nhật các thẻ đếm bằng dữ liệu thật (Nếu có thẻ nào trùng tên nó tự nạp luôn)
                    if (document.getElementById('stat-finance-balance')) document.getElementById('stat-finance-balance').innerText = (data.financeBalance || 0).toLocaleString() + " VND";
                    if (document.getElementById('stat-active-tasks')) document.getElementById('stat-active-tasks').innerText = (data.activeTasks || 0) + " Tasks";
                    if (document.getElementById('stat-total-projects')) document.getElementById('stat-total-projects').innerText = (data.totalProjects || 0) + " Projects";
                    if (document.getElementById('stat-learning-cards')) document.getElementById('stat-learning-cards').innerText = (data.learningCoursesCount || 0) + " Courses";

                    // ==========================================
                    // 🍩 BIỂU ĐỒ 1: TASK DISTRIBUTION BY PRIORITY
                    // ==========================================
                    const donutCanvas = document.getElementById('dashboardDonutChart');
                    if (donutCanvas) {
                        const donutCtx = donutCanvas.getContext('2d');
                        const priorityDataset = data.taskPriorityDataset || [0, 0, 0];

                        new Chart(donutCtx, {
                            type: 'doughnut',
                            data: {
                                labels: ['High', 'Medium', 'Low'],
                                datasets: [{
                                    data: priorityDataset,
                                    backgroundColor: ['#ef4444', '#f59e0b', '#3b82f6'],
                                    borderWidth: 2,
                                    borderColor: '#ffffff'
                                }]
                            },
                            options: {
                                responsive: true,
                                maintainAspectRatio: false,
                                plugins: { legend: { display: false } }, // Ẩn legend gốc vì Tuấn đã có CSS custom legend bên trên
                                cutout: '70%'
                            }
                        });
                    }

                    // ==========================================
                    // 📊 BIỂU ĐỒ 2: STUDY ANALYTICS (WEEKLY LINE CHART)
                    // ==========================================
                    const lineCanvas = document.getElementById('dashboardLineChart');
                    if (lineCanvas) {
                        const lineCtx = lineCanvas.getContext('2d');
                        const studyHoursDataset = data.learningHoursDataset || [0, 0, 0, 0, 0, 0, 0];

                        new Chart(lineCtx, {
                            type: 'line',
                            data: {
                                labels: ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'],
                                datasets: [{
                                    label: 'Accumulated Study Hours',
                                    data: studyHoursDataset,
                                    borderColor: '#2563eb',
                                    backgroundColor: 'rgba(37, 99, 235, 0.1)',
                                    borderWidth: 2,
                                    tension: 0.3, // Tạo độ cong mềm mại cho sóng đồ thị
                                    fill: true
                                }]
                            },
                            options: {
                                responsive: true,
                                maintainAspectRatio: false,
                                plugins: { legend: { display: false } },
                                scales: {
                                    y: { beginAtZero: true }
                                }
                            }
                        });
                    }
                })
                .catch(err => console.error("❌ Lỗi nạp đồ thị tại homepage.ejs:", err));
        });
    </script>
</body>

</html>